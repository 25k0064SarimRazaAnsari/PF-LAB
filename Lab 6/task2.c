#include <stdio.h>

int main()
{
    int no, i, rem;

    printf("Enter your ticket number :");
    scanf("%d", &no);

    while (no32)
    {
        rem = no % 10;
        printf("%d", rem);
        no = no / 10;
    }

    return 0;
}